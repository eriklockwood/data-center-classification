# GitHub Actions CI placeholder.
#
# No jobs run yet by design — we add CI once the team has converged on the
# minimum useful checks (lint, unit tests, notebook smoke tests, etc.).
#
# A reference job is provided below, commented out. To enable: uncomment,
# adjust, and push to a feature branch first to verify it works.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI placeholder — no checks configured yet."

  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v3
  #     - name: Set up Python
  #       run: uv python install 3.11
  #     - name: Sync dependencies
  #       run: uv sync --frozen
  #     - name: Ruff check
  #       run: uv run ruff check .
  #     - name: Ruff format check
  #       run: uv run ruff format --check .
  #     - name: Pytest
  #       run: uv run pytest
